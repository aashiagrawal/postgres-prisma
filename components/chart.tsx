
const Chart = (props: { chartId: any; containerStyle: any }) => {
    const chartId = props.chartId
    const containerStyle = props.containerStyle

    return (
        <div>chart</div>
    )
}

export default Chart